# Занятие 3 · UTM-метки

**Разобрали:** строение ссылки со строкой запроса, три метки `utm_source`,
`utm_medium`, `utm_campaign`, шесть правил справочника, три ошибки написания,
путь одного значения от адреса до строки заявки.

**Сдаётся на паре:** `utm_reference.md` — справочник на четыре канала, четыре
собранные ссылки целиком, четыре тестовые заявки с полями `ep.*` в кадре,
один разбор поломки. Плюс контрольный вопрос устно: показать в Payload
собственное событие и назвать, из какой части адреса взялось `ep.source`.

**Домашних заданий два, сроки разные:**

| № | Файл | Ветка | Срок |
|---|---|---|---|
| 1 | `homework_02.md` | `hw-03` | до начала следующего занятия |
| 2 | `research_02.md` | `hw-03-research` | согласуется с преподавателем |

## Шесть правил справочника

1. только строчные буквы;
2. только латиница;
3. `_` вместо пробела;
4. `utm_medium` — из списка: `organic`, `cpc`, `email`, `social`, `referral`,
   `affiliate`; для QR-кодов договорились писать `qr`;
5. в `utm_campaign` — повод и период;
6. ссылка собирается из справочника, а не по памяти.

Кадры Payload и сохранённых заявок кладите сюда же, рядом с бланками.
